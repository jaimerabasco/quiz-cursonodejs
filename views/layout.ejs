<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <title>Preguntas</title>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" type="text/css" href="/stylesheets/wide.css" media="only screen and (min-width:850px)">
    <link rel="stylesheet" type="text/css" href="/stylesheets/smartphone.css" media="only screen and (max-width:480px)">
  </head>
  <div id="container">
    <header class="caja" id="h1">
    <%if(!session.user){%>
      <span class="right"><a href="/login">Login</a></span>
    <%}else{%>
      <span class="right"> <%=session.user.username%> <a href="/logout">Logout</a></span>
    <%}%>
      <h2>Quiz: el juego de las preguntas</h2>
    </header>
    <main>
      <nav class="caja" id="n1" role="navigation">
          <span><a href="/">Inicio</a></span>
          <span><a href="/quizes">Preguntas</a></span>
          <span><a href="/author">Autor</a></span>
      </nav>
      <section class="caja" id="s1">
        <% if(errors.length){ %>
          <span id="ErrorMsgs">
            Corregir errores:<br>
            <% for(var i in errors) {%>
              <span><%= errors[i].message %></span><br>
            <% } %>
          </span>
        <% } %>
        <div><%-body%></div>
      </section>   
    </main>
    <footer class="caja" id="f1">
      <span><a href="http://es.wikipedia.org/wiki/Quiz">¿Qué es una Quiz?</a></span>
      <span><a href="https://github.com/jaimerabasco/quiz-cursonodejs">Enlace al Proyecto en GITHUB</a></span>
    </footer>
  </div class="container">
</html>
